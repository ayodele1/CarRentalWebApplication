@model DomainObjects.ViewModels.ReservationListViewModel
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="row">
    <div class="col-md-7">
        <table class="table table-responsive table-striped">
            <thead>
            </thead>
            <tbody>
                @foreach (var reservation in Model.Reservations)
            {
                    <tr class="">
                        <td>@reservation.ConfirmationNumber</td>
                        <td><a href="#">View Details</a> | <a href="#" class="btn btn-sm btn-primary">Update</a> | <a href="#" class="btn btn-sm btn-danger">Cancel</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>